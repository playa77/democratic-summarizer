# .env
# Get your key from https://openrouter.ai/keys
OPENROUTER_API_KEY="your api key here"
# --- Model Configuration ---
# You can change the model to any compatible model on OpenRouter.
# Examples: "google/gemini-flash-2.5", "anthropic/claude-4.5-sonnet"
LLM_MODEL_NAME="meta-llama/llama-3.3-70b-instruct:free"

# Controls the creativity of the model.
# For factual summaries, keep this low (0.1 - 0.3).
LLM_TEMPERATURE="0.1"

# The target maximum character length for each text chunk.
MAX_CHUNK_SIZE="16000"

# --- API Call Pacing (in seconds) ---
# Delay between individual API calls for chunks within a single summary run.
DELAY_BETWEEN_CHUNKS="2"

# A longer delay between completing one full summary ratio and starting the next.
DELAY_BETWEEN_RATIOS="5"
